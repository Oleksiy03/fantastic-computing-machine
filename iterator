from Node import Node


class Iterator:
    def __init__(self,head):
        self.current = head
        
    def __iter__(self):
        return self

    def __next__(self):
        if not self.current:
            raise StopIteration
        else:
            o = self.current.data
            self.current = self.current.next
            return o
    
    
