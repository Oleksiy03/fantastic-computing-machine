x=[]
n=int(input("Type in the matrix's size: "))
for i in range(n):
    y=[]
    s=input().split() #ділить по символу роздільника
    for j in s:
        y.append(float(j))
    if len(y)==n:
        x.append(y)

if len(x)==n:
    minel=[]
    for i in range(n):
        if x[i][i]<0:
            #minel.append(min(x[i]))
            m=x[i][0]
            for j in range(1, n):
                if x[i][j]<m:
                    m=x[i][j]
            minel.append(m)

    if len(minel)>0:            
        m=minel[0]
        for i in minel:
            if i>m:
                m=i
        print('the max of the mins is: ', m) 

    else:
       print('there are no rows with negative elements on the main diagonal') #max(minel)
else:
    print('The matrix was incorrectly input. The size is different form what was calimed')
