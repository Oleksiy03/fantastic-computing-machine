def inputMatrix(size):
    print('input the elements of the matrix')
    x=[]
    while len(x) < size:
        y=[]
        s=input().split() #ділить по символу роздільника
        for j in s:
            try:
                y.append(float(j))
            except ValueError:
                pass
        if len(y)==size:
            x.append(y)
    return x

def MaxOfMin(matrix):
    n = len(matrix)
    minel = []
    for i in range(n):
        if matrix[i][i]<0:
                        #minel.append(min(x[i]))
            m=matrix[i][0]
            for j in range(1, n):
                if matrix[i][j]<m:
                    m=matrix[i][j]
                    minel.append(m)
 
    if len(minel)>0:            
        m=minel[0]
        for i in minel:
            if i>m:
                m=i
        print('The max of the mins is: ', m)
 
    else:
        print('there are no rows with negative elements on the main diagonal') #max(minel)
 
while True:
    a=input('input the size of the marix or Q to quit:\n')
    if a == 'q':
        break
    else:
        try:
             n = int(a)
        except ValueError:
            pass
        if n <= 1:
            print("the size isn't supposed to be lesser than 2")
            pass
        else:
            matrix = inputMatrix(n)
            MaxOfMin(matrix)
